info:
  name: User
  type: folder
  seq: 2

docs: |-
  # User (Phase 1)

  Base path: `/api/v1/users`

  Create → get → patch KYC / `preApprovedTransactionLimit`.
  All responses use the standard `ApiResponse` envelope.
